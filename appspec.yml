version: 1
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: sampleTD:2
        LoadBalancerInfo:
          ContainerName: mycontainer
          ContainerPort: 80
