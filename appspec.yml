version: 1
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "sampleTD"
        LoadBalancerInfo:
          ContainerName: "mycontainer"
          ContainerPort: 80
