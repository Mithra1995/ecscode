version: 0.0
Resources:
  - myECSService:
      Type: AWS::ECS::Service
      Properties:
        ClusterName: ecspipelinecluster  # Your ECS cluster name
        ServiceName: sampleTD-service-nudw502z  # Your ECS service name
        TaskDefinition: sampleTD:5  # ECS task definition with the revision number
        LoadBalancerInfo:
          ContainerName: mycontainer  # Container name as defined in the task definition
          ContainerPort: 80  # The container port exposed
