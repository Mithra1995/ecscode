version: 1
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "PLACEHOLDER"
        LoadBalancerInfo:
          ContainerName: "mycontainer"
          ContainerPort: 80
